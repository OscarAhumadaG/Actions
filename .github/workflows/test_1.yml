name: Hello World

on: [push, pull_request]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "Hello, world!"
      - name: Hello world 2
        run: echo "Hello, world 2!"
